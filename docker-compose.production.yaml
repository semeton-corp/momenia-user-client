version: "3.3"

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile.production
    container_name: app-production
    ports:
      - "3000:3000"
    env_file:
      - .env.production
