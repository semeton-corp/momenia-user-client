# Copy file ini jadi .env.local lalu isi nilai aslinya.
# Jangan commit .env.local (sudah di .gitignore).

# URL backend (hanya dipakai di server, tidak kelihatan di browser)
BACKEND_URL=https://your-backend-api.com
